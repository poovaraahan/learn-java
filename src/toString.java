public class toString {
    public static void main(String[] args) {
        myInfo Poovaraahan = new myInfo();
        System.out.println(Poovaraahan.toString());
        // The toString() method is called automatically when you print an object
        // It returns a string representation of the object - its addreess in memory by default
    }
    
}
