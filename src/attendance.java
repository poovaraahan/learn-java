public class attendance {
    void markAttendance(namelist student1) {
        System.out.println(student1.name + " is present.");
    }
    //when u declare a method, u have to have the parameters set up to accept the objects of that datatype
}
